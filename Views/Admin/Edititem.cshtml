@model List<EADWebProject.Models.Ad>
@{
    Layout = null;
    Layout = "~/Views/Admin/Menu.cshtml";
}
    <div class="span3">

    </div>
    <div class="well span9" >
        <h1>Admin Panel: Delete Ads</h1>
            <div class="span9"  >
        <ul class="thumbnails">
            @if(Model != null)
            {
                foreach(var r in Model)
                {
            <li class="span3">
                <div class="thumbnail">
                    <img src="~/Files/@r.file" alt="Sample Image" style="height:200px;width:200px;">
                    <div class="caption">
                        <h4>@r.title</h4>
                        <table>
                            <tr>
                                <td>
                         @using (Html.BeginForm("Details", "Home", FormMethod.Post))
                         {
                             @Html.Hidden("tittle",@r.title);
                            <button type="submit" class="btn btn-info"> Details</button>
                         }  </td>
                        
                                <td>
                         @using (Html.BeginForm("Update", "Admin", FormMethod.Post))
                         {
                             @Html.Hidden("id",@r.Id);
                            <button type="submit" class="btn btn-info"> Update</button>
                         }  </td>
                                </tr>
                            </table>
                    </div>
                </div>
            </li>
                }
            }
            '
            
        </ul>


    </div>

       

